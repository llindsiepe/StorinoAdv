import styled from "styled-components";

export const Container = styled.button`
  background: none;
  color: white;
  border: none;
  border-radius: 4;
  text-align: center;
  font-size: 0.4rem;

  @media (max-width: 720px) {
    font-size: 0.6rem;
  }
`;
