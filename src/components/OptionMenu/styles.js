import * as React from "react";
import { styled } from "@mui/system";

export const Container = styled("button")({
  background: "none",
  color: "white",
  border: "none",
  borderRadius: 4,
  textAlign: "center",
  fontSize: "0.8rem",
});
